// Return a Value from a Function with Return


function timesFive(num){
  return num * 5;
}
var ans = timesFive(5);

// We can pass values into a function with arguments. You can use a return statement to send a value back out of a function as shown in above example.

// Output : ans = 25;